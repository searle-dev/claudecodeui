# Claude Code UI Environment Configuration
# Only includes variables that are actually used in the code

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
#API server
PORT=3008
#Frontend port
VITE_PORT=3009

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================

# 网站访问密码 - 用户需要输入此密码才能访问网站
ACCESS_PASSWORD=claude123

# Session密钥 - 用于加密session数据 (建议生产环境中使用随机生成的密钥)
SESSION_SECRET=your-secret-key-here-change-in-production

# =============================================================================
# PROJECT CONFIGURATION
# =============================================================================

# 项目基础目录 - 用于相对路径创建项目 (默认为用户主目录)
# 例如设置为 /root/projects，那么输入 "my-app" 会创建 /root/projects/my-app
# PROJECT_BASE_DIR=/root/projects